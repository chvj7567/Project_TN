using UnityEngine;

public class CommonEnum
{
    public enum EAddressableKey
    {
        UI,
        Json,
        Scene,
    }

    public enum EUI
    {
        None = 0,
        UIAlarm,
    }

    public enum EAudio
    {
        None = 0,
        BGM,
        Click,
    }

    public enum EJson
    {
        String,
    }

    public enum EFont
    {
        Arvo
    }

    public enum EAtlas
    {
        Item
    }

    public enum EScene
    {
        TestScene,
    }
}
